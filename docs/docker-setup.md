# 🐳 Docker Setup (Ubuntu Server)

This document describes how Docker and Docker Compose were installed and configured on the homelab server.

---

## 📦 Overview

- OS: Ubuntu Server
- Container Runtime: Docker Engine
- Orchestration: Docker Compose

---

## 🔧 Installation

### Update system

```bash
sudo apt update && sudo apt upgrade -y